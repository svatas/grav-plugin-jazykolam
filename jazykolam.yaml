
# v1.4.0
enabled: true
default_locale: ''
prefer_languages_yaml: true

auto_override:
  t: true
  nicetime: true
  gantry: true

# Debug settings
debug:
  enabled: false
  inject: false         # NEW: auto-inject panel+console via onOutputGenerated
  mode: inline          # inline | wrap
  marker_prefix: '‹JL:'
  marker_suffix: '›'
  badge: 'JL'
  max_entries: 200
  console: false
